# ios_adaptive_context_menu example

Run this example app to test the plugin locally.

```bash
cd example
flutter pub get
flutter run -d ios
```

Tap the **Login** text to open the native context menu.
